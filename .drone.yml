kind: pipeline
name: default

steps:
  - name: test
    image: registry.joseluisq.net/rustatic:latest
    commands:
      - pwd -P
      - ls -lah
      - echo $PATH
      - cargo --version
      - rustc --version
      - rustup --version

image_pull_secrets:
  - dockerconfig
